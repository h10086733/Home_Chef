# Home Chef — AI 工作入口

接手时按顺序阅读：
1. [当前状态与待办](docs/CURRENT_STATUS.md)：哪些已实现、哪些仍未上线。
2. [接手指南](docs/AI_HANDOFF.md)：代码定位、运行、测试与资金约束。
3. [文档索引](docs/README.md)：按本次任务选择专题，不必先读全部历史文档。

## 工作约定

- 用户当前明确指令优先。需求文档描述目标，代码及最新验证记录描述实现；发现冲突要说明，不静默改业务规则。
- 保留用户未提交及已暂存的修改。先查看 git status；不要执行 reset、clean 或覆盖不属于本次任务的改动。
- 正常业务必须走真实数据库。不得用演示厨师、假订单、模拟收款、假实名或假保单填充空状态。
- 不读取或输出 .env 的完整内容，不把密钥、密码、OpenID、完整地址、实名资料写进文档、日志或提交。配置只记录变量名称和是否齐备。
- 不重置业务库、不运行演示种子、不改写不可变资金流水。迁移在 db/migrations，schema 在 prisma/schema.prisma。
- 支付、退款和出款必须由服务端核算并核验微信结果；客户端“成功”不能直接更新到账状态。资金改动必须跑对应隔离测试。
- test:wechat、test:integration、test:ui、test:maps 串行运行，避免共享数据库/夹具相互覆盖。不得把测试切换到业务数据库。
- 内部改进、文档和相关验证在已授权范围内直接完成；此文件不额外要求逐步确认。未明确授权时不要对外发送消息或真实资金请求。
- 结尾说明实际修改、实际测试和未验证项。完成代码不等于开通外部权限或已完成真机验收。
- 功能变化更新 CURRENT_STATUS.md；命令/结构变化更新 AI_HANDOFF.md；专题约束更新对应专题。避免复制一份新的“最终状态”造成多头记录。