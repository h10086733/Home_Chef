# 地图接入与启用

更新：2026-09-21。代码接入完成；线上 Key 配额和微信真机联调仍需按真实环境验收。

## 两端行为
- 微信小程序：Taro.chooseLocation 调用微信原生地图，返回地点名称、详细地址及 GCJ-02 经纬度；用户取消不改变原选择，权限拒绝时可自行打开设置。
- 网站与小程序 H5：共用腾讯位置服务地图选点组件，可搜索地点并选点；明确点击“确认这个位置”后才回填表单。无 Key 时显示未配置提示，不使用模拟地图或默认坐标。
- 用户地址：地图位置 + 用户补充的楼栋/单元/门牌号，拼接后通过原接口加密存储于 PostgreSQL；纬度/经度来自所选地点，用于后续服务半径校验。
- 服务者入驻：同样地图选择接单出发位置，代替原先固定默认坐标；完整出发位置不在公开厨师列表展示。
- 地图范围不等于服务开放范围。当前仍是长沙试运营，现有后端使用长沙坐标范围、运营片区与厨师半径进行校验；尚未实现行政区边界多边形自动归属。
- 坐标统一 GCJ-02，不把浏览器原始 WGS-84 定位结果直接用于距离计算。地图组件自行处理定位；允许用户不定位直接搜索。

## 启用网站和 H5
1. 在腾讯位置服务控制台创建应用，申请可用于网页地图选点组件的浏览器 Key，并配置对应授权域名。具体控制台授权要求以当前腾讯文档为准。
2. 将以下配置填入根目录 .env（.env.example 已提供空配置）：
   TENCENT_MAP_BROWSER_KEY=你的浏览器Key
   TENCENT_MAP_REFERER=你的应用名称
3. 重启 API 服务。前端通过已登录接口 GET /maps/config 获取这两项公开客户端配置，不需要重新构建。
4. 开发联调时按实际访问方式配置 localhost / 127.0.0.1；部署后配置实际网站和 H5 域名，使用 HTTPS。不要填入服务端 Secret、签名密钥或微信 AppSecret。
5. 网站“服务地址”和 H5“我的 → 服务地址”点击地图选址，搜索真实小区，确认地点、补充门牌号并保存。网络失败或 Key 授权失败时，可关闭地图检查配置后重试。

浏览器 Key 会发送给地图 iframe，是公开客户端配置；应在地图控制台限制其用途、授权域名和配额。项目不会读取或发送其他 .env 凭据。
腾讯选点组件协议与参数同时参考了已安装的 @tarojs/taro-h5 4.0.9 官方实现（chooseLocation.js）。

## 配额故障排查

- `status: 121` / `此key每日调用量已达到上限` 表示腾讯位置服务因日配额限制拒绝本次请求。先在腾讯位置服务控制台核对该 Key、对应接口及账号的日配额和今日用量；若用量很少，检查可用额度是否为零或自设限额过低。根据控制台实际情况调整自设限额、申请/购买额度，或等待下一配额周期。不要用重试循环缓解，也不能假设换 Key 就能恢复账号共享额度。
- `TENCENT_MAP_BROWSER_KEY` 是公开发给网页选点 iframe 的浏览器 Key。若要直接调用 `place/v1/suggestion` 等 WebService API，应在控制台为对应 Key 开通 WebServiceAPI，并按用途单独配置和限额；当前应用没有服务端代理该接口。
- 浏览器 Key 本来就会发送给客户端；应限制授权域名、服务类型和配额。若控制台用量异常，再排查共用应用、请求来源和是否需要轮换。调整腾讯侧配额无需改项目配置；只有更换 Key 时才更新 `.env` 并重启 API。
- `TENCENT_MAP_REFERER` 是选点组件 URL 中的应用名称参数，和 curl 的 HTTP `Referer: https://chushidao.com` 请求头不是同一项配置。当前响应报告的是配额限制；恢复额度后仍需验证域名授权及真实网页选点，不能据此认定其他配置已全部通过。
- 历史响应 `status: 199` 附带“此 key 未开启 WebserviceAPI 功能”；本次 `status: 121` 附带日配额提示。应结合实际 `message` 排查，不能沿用上次故障结论。

2026-09-21：用户报告长沙“五一广场”地点输入提示接口返回 121。本机使用本地配置 Key、HTTP Referer 为正式网站域名进行一次真实查询，同样返回 121；只输出状态码和错误消息。已核对代码：网页/H5 打开腾讯 `tools/locpicker` iframe，小程序使用原生 `chooseLocation`；应用没有直接调用 `place/v1/suggestion`，也没有该接口的自动重试循环。腾讯控制台用量、可用配额及恢复后的实际选址尚未验证。

## 微信小程序
- 不需要通过网页 iframe 获取位置，使用微信原生地图接口。
- app.config.ts 已声明 requiredPrivateInfos: chooseLocation 及 scope.userLocation 用途说明。
- 替换 touristappid，使用真实小程序 AppID，在微信后台按实际用途配置位置接口能力、权限及用户隐私保护指引，核实账号可调用相关接口。
- 完成 API 的 HTTPS 域名部署与请求合法域名配置。
- 真机验证首次授权、拒绝/恢复授权、取消选址、搜索地点、保存地址及预约范围校验。这里只完成代码和编译，不代表微信审核或能力开通已经完成。

## 验证与限制
- 全项目类型检查通过；网站/API 构建通过；微信小程序、H5 构建通过（现有资源体积提示）。
- tests/maps-browser.mjs 使用浏览器路由拦截的地图测试替身，测试实际 iframe 消息来源/窗口验证、无效坐标、未配置 Key、取消、确认，覆盖网站与 Taro H5。
- 选中后的中文地址、门牌号与六位小数坐标通过真实 API 写入实际 PostgreSQL，并回读断言一致；测试地址随后删除，审计保留。
- 同时验证范围外地址、非 GCJ-02 坐标系请求被拒绝。
- 原有 15 个业务场景（测试执行器共计 16 项，含父测试）全部通过。
- 运行：bash scripts/pnpm.sh test:maps（自动准备网页/H5及3001隔离测试API，使用 home_chef_test，不使用业务库）。
- 测试替身只在验收脚本里，不在应用中，不代表真实腾讯地图服务/微信设备已验收。真实搜索、定位、额度与域名授权必须取得 Key 后联调。
- 图片 map-web-fixture.png / map-mini-fixture.png 为测试地址回填截图，不是实时地图截图。

参考：
- Taro 原生位置接口与 GCJ-02 字段：https://docs.taro.zone/en/docs/apis/location/chooseLocation/
- 腾讯地图选点组件：https://lbs.qq.com/webApi/component/componentGuide/componentPicker

## 2026-09-15 Key 联调更新
用户已提供浏览器 Key，已写入本地 .env 并重启 API；不在文档记录 Key。Windows 端腾讯选点页面 HTTP 200；Chromium 中实际 iframe 尚未加载出可操作地图，未完成真实选址。公开行政区地理编码探测返回 status 199：此 key 未开启 WebserviceAPI 功能。需确认地图控制台中该 Key 的服务启用与域名授权后继续联调；此响应不单独证明 iframe 空白的原因。

## 2026-09-22 本地真实组件复现

- 启动本地 Vite 网页，Chromium 调用项目实际 chooseBrowserLocation，使用本机地图配置；未拦截腾讯请求、未写业务数据。真实组件出现“获取消息列表失败”。
- 组件入口 `apis.map.qq.com/tools/locpicker` 返回302，实际页面为 `https://mapapi.qq.com/web/mapComponents/locationPicker/v/index.html`。
- 组件内部 `h5gw.map.qq.com/ws/place/v1/search` HTTP200，但业务 status110，message明确为“来源域名未被授权”，来源域名 `mapapi.qq.com`。HTTP200不代表选址成功。
- 单次地点输入提示查询（HTTP Referer为正式网站域名）返回status0；本次未复现昨日121，不能继续把当前故障认定为日配额。
- 腾讯控制台对应Key → WebServiceAPI → 域名白名单：保留现有网站授权，增加 `mapapi.qq.com`（每行一个域名，不带协议和路径）。官方组件文档明确要求白名单增加组件域名；不要关闭整个白名单或广泛授权qq.com。保存后重新打开选点页面验收。
- 代码同步修复：消息接收允许 `https://apis.map.qq.com` 和 `https://mapapi.qq.com` 两个精确来源，仍要求来自当前iframe窗口且坐标/地址有效；否则即使腾讯恢复，重定向后选点消息也会被旧校验丢弃。
- 回归用例增加H5组件跨域跳转（替身用页面导航模拟，真实302已单独观测），并验证相似恶意域名、HTTP来源及其他窗口均不能回填。自动化仍使用地图替身，只验证我方逻辑，不冒充腾讯恢复证明。
- 本机地图瓦片请求HTTP200，但默认IP定位落在海外；尚不能认定用户截图底图空白只有一个原因。需白名单修复后搜索长沙地点，再验收底图、地点列表和确认回填。未操作用户腾讯控制台，未完成恢复后的真实选址。
2026-09-22 修复后验证：test:maps通过（含业务夹具7项、网站旧域名/H5新域名跨域回填、安全来源与隔离数据库保存）；网页和小程序直接tsc检查通过，git diff --check通过。腾讯白名单恢复后的实时选点未验证。

### 2026-09-22 白名单修改后的后续验证

- 真实组件周边检索已返回status0，110不再出现。过程中搜索提示曾返回121，而随后同一配置Key在组件内的长沙搜索也返回0并展示结果；记录以请求时点为准，不能继续将额度错误表述为持续阻塞，也不能保证后续配额始终可用。
- 用长沙公共地点作为本次测试初始中心（未改业务默认定位），真实点击“五一广场”：收到origin=https://mapapi.qq.com、source为当前iframe的消息，坐标类型为number，名称与地址有效；本地确认按钮启用，点击后弹窗关闭。没有拦截腾讯响应，没有保存业务地址。
- 只读检查 https://chushidao.com 当前加载的公开脚本，存在旧来源apis.map.qq.com，不含新来源mapapi.qq.com；线上尚未包含本地兼容修复。这会使腾讯列表已选中，但外层“确认这个位置”仍禁用。
- 网页正式构建通过，产物 apps/admin-web/dist；待获得实际部署方式/服务器目录后更新线上，并再次验收。此次未发布线上，不能将本地成功等同正式网站已修复。

2026-09-23：网页预约新增 AddressForm，复用现有 MapField 与 /addresses 接口；保存后自动选择地址并预检厨师范围。/chefs 支持地址片区与坐标筛选，按运营半径过滤且排除关闭片区。test:maps已覆盖预约内新增、数据库回读、自动选中和时间保留；范围规则测试通过。详见 CURRENT_STATUS.md。
