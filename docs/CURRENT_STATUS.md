# 当前状态与待办

最后核对：2026-09-18。此文件是当前实现状态入口；早期 PRD、方案和验收矩阵不是完成证明。运行中服务、配置和数据库连接状态需每次接手重新检查。

## 产品与阶段

厨临门：面向日常家庭消费的上门做饭服务，帮助有手艺的家庭主妇等服务者获得收入。参考 51 厨师的发现、详情和预约体验，但定位为亲民家常服务。当前目标：可试运营 MVP。

目前处于“主要业务代码及本地回归完成，正式部署与真实渠道验收未完成”阶段。业务模式为 business，使用真实 PostgreSQL；不能宣称全部功能已完成或只填 Key 即可上线。

## 能力状态

| 能力 | 当前实现 | 验证/限制 |
|---|---|---|
| 账号与权限 | 密码注册、登录、会话、角色校验、独立财务授权 | 已接微信手机号一键登录/旧账号绑定，UID稳定，号码加密；真实权限/真机待验收，短信及第三方实名未接 |
| 厨师 | 材料上传、人工审核、试岗、套餐、档期 | 上传和人工审核不等于外部实名认证；培训/季度复审不完整 |
| 发现与地址 | 数据库搜索、菜系/价格/距离筛选、收藏、地图选址 | 腾讯组件/微信原生地图已接；线上域名与真机权限待验收；地理校验仍限长沙 |
| 片区 | 网页、小程序地址和入驻读取运营开放片区；关闭片区禁用提交 | 动态选择及保存通过测试；未实现地区多边形判定 |
| 预约履约 | 报价、待支付、邀请/接单、档期互斥、履约凭证、确认、改约、评价 | 广泛本地回归；历史履约资金用例含 sandbox，不能等同真实微信验收 |
| 收款与退款 | 定金、全款、尾款、采购增量、原路退款；验签/查单/幂等 | 真实微信代码；商户配置和小额真机收退款待验证 |
| 采购增量 | 差额由服务器计算，绑定变更 ID；到账才更新预算；迟到无效付款退款 | 自动化通过，原尾款保持不变；网页/H5到小程序付款 |
| 厨师收入 | 默认余额结算→提现审核→微信转账；选配直接分账 | 配置/权限/真机到账待完成；两条路径不能重复出款 |
| 售后 | 工单、建议/批准/执行分权、退款限制 | 裁定后剩余收入分配、已出款追偿、超期人工核销仍未闭环 |
| 通知与沟通 | 站内消息、订单聊天、敏感内容拦截、评价 | 未接微信订阅消息或短信，用户离开页面可能漏单 |
| 保险 | 业务模式不创建假保单 | 未接真实投保和理赔 |
| 运维 | JSON运行日志、关联审计、部署模板、自检、备份脚本 | 生产部署、日志留存/告警、备份恢复演练仍待正式验证 |

图片目前真实存入数据库，不是对象存储。网页/H5可下单，不提供独立 H5 收银台或扫码支付。

2026-09-21 地图专项核对：本地配置的腾讯 Key 在真实地点输入提示接口查询中返回 `121`（每日调用量达到上限）。需在腾讯控制台核对日配额与用量并恢复额度，再验收网页/H5真实选点；尚未验证微信原生地图是否受影响。排查记录见 [地图接入与启用](map-integration.md)。

## 下一步工作队列

1. **消息触达**：接单、支付、改约、服务提醒的微信订阅消息；先完成模板映射、用户授权、失败重试和幂等，模板 ID 等由平台准备。
2. **售后资金闭环**：裁定后剩余收入分配、已结算订单的追回/冲正、超期提现人工核查；明确权限和审计，不用“标记已处理”代替真实资金结果。
3. **真实部署验收**：正式服务器/域名、真实 AppID、支付商户绑定、转账场景/分账比例、回调及 worker，按小额订单逐项确认。
4. **运营保障**：日志留存与告警、备份恢复演练、异常订单处理与独立客服/财务账号配置。
5. **后续产品缺口**：短信入口、第三方实名、保险、更多片区及素材存储；这些需要外部能力或业务决定。

以上为接手排序建议，用户的新指令优先。不要因为本清单自动发送消息、扣款或申请外部服务。

## 需明确的规则差异

- 历史增强需求对齐记录写“确认后检查结算资格，无额外固定等待期”；当前 chef-finance.ts 实施确认后至少 168 小时保护期，最高 720 小时。当前行为保留；调整需明确业务决策并覆盖售后与资金测试。
- 15% 是现有初始化佣金规则，订单使用规则快照，不代表全部业务长期固定为 15%。
- business 禁止模拟付款/退款/结算/保险，历史 sandbox 代码仍用于隔离回归；不要误报为已经彻底删除，也不要把它接回真实业务。

## 最近验证记录

2026-09-17 最近相关验证：类型检查和全部 7 个包构建通过；网页/H5业务及财务浏览器回归通过；地图夹具回归通过（选址、非法来源/坐标、落库、动态片区、关闭片区）。后台配置测试通过。

此前同日全量：7 个包单元任务通过；业务数据测试 7 项、微信资金测试 29 项、历史综合测试 16 项通过，计数含父测试。代码变化后按影响重跑，不能把历史通过当作新提交证明。微信测试使用隔离库、测试签名和模拟传输，没有真实资金交易；地图夹具不证明腾讯线上或微信真机已验收。

验证细节：[日志与功能验收](logging-and-acceptance.md)。历史本地日志在 .data/，可能被覆盖；不随仓库分发，不是永久凭证。
2026-09-18：微信手机号一键登录接入，详见 [微信登录说明](wechat-login.md)。新增字段迁移已应用本机业务库，保留既有账号和业务数据。

微信登录专项 8 项、支付回归 29 项通过（含父测试）；类型检查、7 个包构建及网页/H5原有流程通过。登录测试使用受控微信响应，不代表真实手机号能力已开通。发布自检已增加登录开关和 AppSecret 检查。

2026-09-22 地图真实复现：本地Chromium打开项目选点组件，腾讯内部地点搜索返回110（来源mapapi.qq.com未授权），页面显示“获取消息列表失败”；正式域名来源的单次输入提示查询返回0，昨日121本次未复现。已兼容腾讯302跳转后的精确消息来源，保留窗口与坐标校验；控制台需增加组件域名白名单，恢复后的真实选址仍待验收。详见地图专题。
2026-09-22 修复后验证：test:maps通过（含业务夹具7项、网站旧域名/H5新域名跨域回填、安全来源与隔离数据库保存）；网页和小程序直接tsc检查通过，git diff --check通过。腾讯白名单恢复后的实时选点未验证。

2026-09-22 后续真实地图验证：白名单生效，长沙周边及最新搜索请求成功；真实点击公共地点后，本地收到mapapi.qq.com消息，确认按钮可用且弹窗正常关闭，未写业务地址。线上公开脚本仍不含新域名修复；网页正式构建通过（apps/admin-web/dist），待实际部署入口发布并验收线上。期间出现过搜索121，最新请求已成功，详见地图专题时间顺序。

2026-09-23 预约地址与服务范围：
- /chefs 列表按当前运营 radiusM（与新报价一致）过滤定位距离，排除已关闭片区；支持 regionCode 过滤。此为统一已有下单规则，未调整运营配置或历史订单快照。
- 网页预约可直接地图新增地址、补充门牌号，真实保存后自动选中，保留预约时间等字段；新增前释放已有报价。切换地址后按该地址坐标和片区预检厨师，范围不符或校验未成功时禁用获取报价。
- 后端将服务范围不符与其余预约资格/时间不可用分开提示。首页未指定预约时间，列表不承诺具体档期可约；报价仍执行完整档期、缓冲时间和资格校验。地理范围仍按现有坐标半径与片区代码，不具备行政区多边形校验。
- 本次验证：隔离业务测试8项（含父测试）通过；test:maps网站/H5回归通过，新增覆盖预约内地图地址落库与自动选中、预约时间保留；网页和API直接类型检查及正式构建通过。地图测试使用替身，未新增真实腾讯/微信真机验收。
- 尚未部署线上。需提交并推送本次改动后，在服务器更新代码、构建网页和API并重启API，网站须指向新的静态产物。