{
  "name": "@home-chef/domain",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "lint": "tsc -p tsconfig.json --noEmit", "test": "tsx src/matching.test.ts && tsx src/order.test.ts" },
  "devDependencies": { "typescript": "^5.7.0", "tsx": "^4.19.2" }
}
